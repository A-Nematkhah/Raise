[pytest]
testpaths =
    raise_core/tests
    domains/crowdnav/tests
    domains/highway/tests
pythonpath =
    .
    domains/crowdnav/runtime
markers =
    slow: real simulator / long-running checks (deselected by default in CI via -m "not slow")
filterwarnings =
    ignore::DeprecationWarning
